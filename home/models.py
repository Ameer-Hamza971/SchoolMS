from django.db import models
# ------------------------------------------------------------------
# Custom Forms
from django.contrib.auth.models import AbstractBaseUser, BaseUserManager
# ------------------------------------------------------------------
# Create your models here.
# class Contact(models.Model):
    # fst_name = models.CharField(max_length=50)
    # lst_name = models.CharField(max_length=50)
    # email = models.CharField(max_length=120)
    # std_id = models.CharField(max_length=100)
    # camp = models.CharField(max_length=100)
    # p_code = models.CharField(max_length=100)
    # mesg = models.TextField(max_length=300)
    # date = models.DateField()
    # def __str__(self):
    #     return (self.fst_name  + " " + self.lst_name) 





